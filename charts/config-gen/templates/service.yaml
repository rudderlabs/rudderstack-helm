apiVersion: v1
kind: Service
metadata:
  name: {{ include "config-gen.fullname" . }}
  labels:
    {{- include "config-gen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "config-gen.selectorLabels" . | nindent 4 }}
